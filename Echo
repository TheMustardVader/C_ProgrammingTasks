#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main()
{
    char str[100];
    int i;

    do{
        fgets(str,100,stdin);
        for(i=0;i<'\0';i++);
        printf("%s",str);
    }while(EOF);
    return 0;
}
